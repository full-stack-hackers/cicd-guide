version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.6

    working_directory: ~

    steps:
      - checkout

      - run: ls
      - run:
          name: install dependencies
          command: npm install

      - run:
          name: run tests
          command: npm test